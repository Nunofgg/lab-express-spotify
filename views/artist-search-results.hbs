<ul>
    <div class="row container artists-results">
        {{#each artists}}
            <div class="col-md-4"> 
                <div class="card" style="width: 18rem;">
                    <img src="{{images.1.url}}" class="card-img-top" alt="{{name}}">
                    <div class="card-body">
                        <h5 class="card-title">{{name}}</h5>
                        <a href="/albums/{{id}}" class="btn btn-primary">View Albums</a>
                    </div>
                </div>
            </div>
        {{/each}}
    </div>
</ul>